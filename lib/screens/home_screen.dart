import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('NaijaCopas Home')),
      body: Center(child: Text('This is the real home page')),
    );
  }
}
